const character = 'luigi';

console.log(character)

const inputs = document.querySelectorAll('input')

console.log(inputs)

inputs.forEach(input => {
  console.log(input)
})

// compiling - tsc <filename> -w  // w -> watches the ts file for changes
